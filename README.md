# Test ws app [Draft version]


To start server in the `server` directory, run:

```
cd server
yarn
yarn start
```

To start client in other terminal in the `client` directory, run:

```
cd client
yarn
yarn start
```